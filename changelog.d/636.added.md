Stand up the `aces-adapters` monorepo: independent per-simulator adapter
projects (`sim_adapter_base`, `cyborg_adapter`) with isolated lockfiles, a
per-adapter CI matrix, full Ground Control onboarding, and a strict SonarCloud
quality gate mirroring `aces-strict`. Implements REP-002 per ACES ADR-069.
