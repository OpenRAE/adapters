Stand up the `raes-adapters` monorepo: independent per-simulator adapter
projects (`sim_adapter_base`, `cyborg_adapter`) with isolated lockfiles, a
per-adapter CI matrix, full Ground Control onboarding, and a strict SonarCloud
quality gate mirroring `raes-adapters-strict`. Implements REP-002 per RAES ADR-069.
